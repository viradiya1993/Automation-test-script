import { createAction } from '@ngrx/store';

export const startProgressBar = createAction('[ProgressBar Component] Start');
export const stopProgressBar = createAction('[ProgressBar Component] Stop');
